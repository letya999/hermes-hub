CHG-0036 reconnect correction and live evidence: Hermes `/v1/runs` treated `/reload-mcp` as model input, so a completed run was a false positive and is no longer used as reconnect proof. The Go runtime watcher now applies a ToolHub projection revision through its existing controlled restart loop, preserving the owner `/state` volume and recording the applied revision to prevent repeat restarts. On revision 42, the live Hermes gateway process was replaced inside the same runtime container; the pre-existing owner session ID still returned 200 and runtime health returned healthy. This proves cold reconnect with session persistence. In-process hot reload remains dependent on a native Hermes control endpoint and is not claimed here.
