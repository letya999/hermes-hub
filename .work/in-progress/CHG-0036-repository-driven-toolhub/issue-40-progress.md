CHG-0036 generated Node build and initialize/tools/list passed for zereight/gitlab-mcp@3e826c33a0c9a6cf4b5626450cbfa668f0984bd0. Two bin aliases are treated as one entrypoint; upstream RUN cache mounts are not installation authority. The existing owner connection was reused during upgrade. Fixed common workload-release routing and revocation of the old Broker grant before replacement. Live whoami remains blocked: the configured API hostname fails DNS both on Windows and inside the egress container. The intended URL/VPN was requested from the owner; no fallback host or egress weakening was applied. Actual credential rotation plus successful whoami is still required.
