VPN follow-up: after the owner enabled VPN, ToolHub whoami passed. Protected Broker re-enrollment rotated the same owner connection to revision 3; post-rotation whoami also passed. No duplicate owner connection was created and no API-host fallback was used. The first post-start call exposed a common readiness race, now fixed locally by probing MCP initialize/tools/list instead of merely an HTTP listener; its focused race regression passes. Final gates are being repeated after that fix.
