CHG-0036 live evidence for the exact nspady/google-calendar-mcp source: generated restricted Node recipe built with entrypoint `build/index.js`; isolated MCP initialize/tools/list passed. The existing OAuth client went through the protected Broker form. After explicit owner authorization for a repeat Google OAuth flow, the owner token state was checkpointed by Broker. Real projected ToolHub `list-calendars` and `list-events` both passed. Provider response bodies and OAuth credentials were not posted here. The prepared entry remains Calendar-only; full Google Workspace is a separate catalog decision.
