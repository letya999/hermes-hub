schema: 1
id: CHG-0037-toolhub-closeout
status: in-progress
phase: owner-isolation
started_at: "2026-09-23"
updated_at: "2026-09-23"
completed:
  - "Current main and issue gaps reviewed; work continues on feat/toolhub-closeout."
  - "Broker runtime now rejects a missing required delivery despite other mounts; cleanup does not falsely claim quiescence."
  - "First binding uses readiness before projection; retry after admission failure reuses the existing owner connection."
  - "File-credential readiness admits and releases the workload before state checkpoint."
  - "Four exact-source entries define reviewed safe provider probes; mock regressions reject MCP error results."
  - "Prepared provider readiness now rejects MCP error results, missing probes, invalid controller receipts and unquiesced file-credential workloads."
  - "Official MCP Registry name search returned real unprepared candidates; Docker MCP name search and owner-bound selection have focused regressions."
  - "Selected OCI artifacts recheck exact source and digest; revision and version drift regressions pass."
  - "Current just check passed with 85.05% own Go statement coverage, race tests, format, vet, staticcheck and docs checks."
  - "Docker image build, standalone smoke, pinned Hermes 0.21.0 contract, five-minute lifecycle and cleanup passed separately after interrupted observation; optional hub-stt fixture unavailable."
  - "Real public Docker MCP OCI metadata verification passed; full unprepared registry install is still pending."
  - "Go gateway now keeps token-isolated stateful MCP sessions, sends list_changed on projection updates, and denies stale/cross-owner calls; focused wire and package tests pass."
  - "Serve-runtime Hermes restart watcher was removed; ToolHub process polls persisted projection changes."
  - "Pinned Hermes 0.21.0 Docker fixture observed dynamic removal/restoration, same PID/session, an active run completing through refresh, and cold ToolHub endpoint recovery."
  - "ToolHive and GitHub fallback name search passed real public API probes; Smithery search, fallback ordering, provenance merging and adapter isolation passed fixture regressions."
  - "Live GitHub-search discovery returned five unprepared echo candidates; one source completed generated Node build, MCP initialize/tools/list, safe echo call and disable/remove with tool absence in the same session."
  - "Broker-only injection now permits bindings whose manifest has no credential fields, while local refs and missing required credential refs remain denied; focused regressions pass."
  - "Pinned repository recipe metadata preserves repo-relative paths so a subfolder is applied exactly once; the regression and live generic install pass."
  - "Post-fix just check passed race/shuffle tests, formatting, vet, staticcheck, docs and actionlint at 85.01% own Go statement coverage."
  - "Post-fix just docker-check passed image build, standalone smoke, native approval, layered health, orphan inventory, supervisor, gateway five-minute cold restore and pinned Hermes 0.21.0 contract; optional hub-stt fixture is absent from the image."
next:
  - "Complete an unprepared Official MCP Registry/marketplace candidate through the full generic lifecycle."
  - "Run two-owner Docker/Broker acceptance and live provider probes without outputting credentials or payloads."
  - "Complete #74 direct/prepared install, revoke/remove, conflicts, failure/timeout and restart acceptance against real Hermes."
  - "Complete remaining provider/bundle scope and production Telegram journey."
risks:
  - "Mock provider probes do not prove real account access; live acceptance is still required."
  - "Do not close issues until every issue-specific criterion has current evidence."
